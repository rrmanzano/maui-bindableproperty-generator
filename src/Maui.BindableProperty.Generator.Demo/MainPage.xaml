<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Maui.BindableProperty.Generator.Demo.MainPage"
             xmlns:controls="clr-namespace:Maui.BindableProperty.Generator.Demo.CustomControls"
             x:Name="this">

    <ScrollView>
        <VerticalStackLayout Spacing="25" Padding="30">

            <controls:HeaderControl
                FirstName = "{Binding BindingContext.FirstName, Source={x:Reference this}}"
                LastName = "{Binding BindingContext.LastName, Source={x:Reference this}}"
                BirthDate = "{Binding BindingContext.LastName, Source={x:Reference this}}"
                Country = "{Binding BindingContext.Country, Source={x:Reference this}}"/>

            <Button 
                Text="Clear"
                FontAttributes="Bold"
                SemanticProperties.Hint="Save data"
                Command="{Binding SaveClickedCommand}"
                HorizontalOptions="FillAndExpand" />

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
